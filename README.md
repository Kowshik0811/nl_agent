# SnowFlare Sensor Agent

This project allows querying sensor data using **natural language**.  
It supports both **Python CLI** and **Streamlit UI**.

## Features

- Natural language queries for sensor readings and status
- Supports active/inactive sensors
- Supports temperature, humidity, and timestamp queries
- Dynamic Streamlit UI


